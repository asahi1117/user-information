# Week11 AIと考えたログ

## Part 1：AIと考えた1セッション

### テーマ

Week10で書いた GitHubユーザー情報表示ツール（`index.html`）のうち、`fetch`・エラー処理・検索ボタンの無効化の書き方をレビューしてもらい、直すべき点と直さなくてよい点を切り分けた。

### 送った指示（良いプロンプトの型）

目的:
Week10の GitHubユーザー検索の非同期処理を、自分が説明できる粒度まで理解しつつ、改善点だけを洗い出す。完成コードの差し替えは求めない。

前提:
単一の `index.html`。ユーザー名を入力して `https://api.github.com/users/{username}` に `fetch` し、成功時はカード表示、失敗時は「ユーザーが見つかりませんでした」を出す。`async/await` と `try/catch`、検索中のボタン無効化を使っている。個人情報やトークンは使っていない。

見てほしい観点:
- `fetch` と `response.ok` の使い方が意図どおりか
- エラー時にユーザーへ何が伝わるか（404以外も含む）
- 検索連打を防ぐ書き方が妥当か
- 学習用の `console.log` を残すかどうか

制約:
- 完成ファイルを丸ごと書き直さない
- 指摘は「何を・なぜ・優先度」まで書く
- GitHubトークンなど秘密情報をブラウザに置く案は出さない
- 新しいライブラリは増やさない

出力形式:
1. 良い点（残してよい理由つき）
2. 改善候補（優先度つき、コードは短い断片だけ）
3. やらなくてよいこと

### 返ってきた出力（要約）

良い点として、空文字チェック、`encodeURIComponent` でのユーザー名の埋め込み、`response.ok` で失敗を拾っていること、`try/catch/finally` で検索ボタンを必ず戻していることが挙げられた。

改善候補は次のとおりだった。

- `catch` ですべての失敗を「ユーザーが見つかりませんでした」にしている。ネットワーク切断や API のレート制限（403）も同じ文言になる
- `404` とそれ以外でメッセージを分けた方が、何が起きたか説明しやすい
- 学習用の `console.log` は提出後も残すなら、本番向けには外すか減らす
- 連続検索で前のリクエストが残る場合に備え、`AbortController` で中断する方法がある

やらなくてよいこととして、クライアントから Personal Access Token を付ける案、Tailwind をやめて CSS を全部書き直す案は不要、と返ってきた。

### "なぜ"を聞いた記録

- 聞いたこと:
  `handleSearch` の最後で、成功時でも失敗時でも `searchButton.disabled = false` を `finally` に書いているのはなぜか。`try` の末尾と `catch` の末尾にそれぞれ書けば足りないのか。また、URL を作るときに `encodeURIComponent(username)` を使うのはなぜか。

- 返ってきた説明（要約）:
  `finally` は `try` が成功しても `catch` に入っても、途中で `return` しても必ず実行される。ボタン解除を `try` の成功パスだけに書くと、エラー時にボタンが押しっぱなし（無効のまま）になる。`catch` にも書けば動くが、同じ後始末が2箇所になり、片方を忘れやすい。
  `encodeURIComponent` は、ユーザー名に `/` や空白、日本語などが入ったときに URL の形が壊れたり、意図しないパスになったりするのを防ぐ。GitHub のログイン名は通常英数字だが、入力欄は自由に打てるので、埋め込み前にエンコードするのが安全、という説明だった。

- 聞く前は分からなかったが、聞いて分かったこと:
  `finally` は「成功・失敗のどちらでも必ずやる後始末」のための場所だと説明できるようになった。以前は「よく見る書き方だから」としか言えなかった。`encodeURIComponent` も「おまじない」ではなく、入力を URL の一部にするときの逃げ方だと分かった。

### 出力の4分類

| 分類 | 内容 | 判断理由 |
|---|---|---|
| 採用する | 空文字チェック、`response.ok` での失敗判定、`finally` でのボタン再有効化、`encodeURIComponent` | 自分が説明できるようになったうえで、今の要件（検索・エラー表示・連打防止）を満たしているため |
| 保留する | 学習用の `console.log` を減らす／消す | Week10の確認（取得データの中身を見る）にはまだ役立つ。提出後に消すかは別判断にしたい |
| 採用しない | ブラウザの JavaScript に GitHub トークンを置いてレート制限を回避する案 | トークンが画面を開いた人に見える。課題の範囲でも秘密情報をクライアントに置く必要がない |
| 要確認 | 404以外（通信エラー・403）でも同じ「ユーザーが見つかりませんでした」を出す点。`AbortController` でリクエスト中断するか | 課題の必須要件は「見つからないユーザー」の表示なので、今すぐ必須ではない。通信失敗と 404 を同じ文言にしてよいかは、仕様として確認したい |

---

## Part 2：流された場面の振り返り

Week10で GitHubユーザー情報ツールを作るとき、`fetch` から画面反映までの完成コードを一度に受け取り、`finally` や `encodeURIComponent` の意味を説明できないままファイルに入れた。動くことが先で、「なぜその行があるか」を後回しにしたのが流された理由だと思う。

---

## Part 3：今後のAI利用方針

### Week11で気づいたこと

AIの出力は「正しそうに見える」ことと「自分が説明できる」ことは別だと分かった。`finally` のように短い行ほど、聞かないと理由が残らない。4分類すると、便利そうでも採用しない・要確認を残せる。

### 自分のAI利用ルール

1. 完成コードを丸ごともらって貼らず、自分で書けない行は必ず「なぜ」を聞く
2. 返ってきた案は採用・保留・採用しない・要確認に分け、採用しない／要確認をゼロにしない
3. 個人情報・トークン・課題の非公開教材全文は AI に貼らない
